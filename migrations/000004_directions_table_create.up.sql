CREATE TABLE IF NOT EXISTS "directions" (
                                            "id" serial PRIMARY KEY,
                                            "name" varchar,
                                            "description" varchar
);
